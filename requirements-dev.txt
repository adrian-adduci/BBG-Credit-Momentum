# Development and test dependencies.
# Install after requirements.txt:  pip install -r requirements-dev.txt

-r requirements.txt

pytest==8.3.3

# Required by fastapi.testclient / starlette.testclient.
# MUST stay below 0.28. starlette 0.35.1 (pinned transitively by
# fastapi==0.109.0) constructs its TestClient with httpx.Client(app=...), and
# httpx removed the `app` shortcut in 0.28.0 in favour of an explicit
# WSGI/ASGI transport. With httpx 0.28.x every API test errors at fixture
# setup with:
#     TypeError: Client.__init__() got an unexpected keyword argument 'app'
# Lifting this pin requires upgrading fastapi/starlette together.
httpx==0.27.2

# ppscore==1.3.0 imports pkg_resources, removed in setuptools 81+.
# Pinned here so `pip install -r requirements.txt` can build it.
setuptools<81
